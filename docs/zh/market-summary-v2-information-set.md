# 市場摘要 v2 — 資訊集設計報告

> 撰於 2026-07-19。源於使用者在首筆紀錄落地當天的發現(見 §1),定案摘要 v2 規格並記錄設計理由。
> 相關:[回測污染分析_雙層記憶.md](回測污染分析_雙層記憶.md)、[preregistration.md](preregistration.md) §8 增補紀錄、知識庫 [[LLM回測的雙層記憶污染]]。

---

## 1. 發現:v1 摘要在測試「被廢掉武功的人格」

v1 摘要只含 24 小時切片(現價/日高低/昨收/成交量)。但 ICT 這類人格的框架**本質是 HTF 敘事驅動**:日線/週線 PD array 決定 bias,LTF 只是進場擇時。只給 24h 切片,等於要求人格在被剝奪核心工具的狀態下出判斷。

**第一手證據**:首筆紀錄(2026-07-19)中,ICT 人格自行指出「四個維度我只摸到半個」「it's a guess, it's not scientific」,給出 Neutral/25 的低信心——人格自己抱怨了輸入不足。以 v1 輸入累積三個月,測的是「框架被砍掉 HTF 後還剩多少」,這個問題沒有價值。其他人格同理:均線派看不到均線排列、位階派看不到大級別壓力,任何框架都無從施展。

## 2. 為什麼補歷史走勢不構成記憶污染

**look-ahead bias 的定義是「判斷時用到判斷日之後的資訊」**。給模型判斷日**之前**的 90 天日線、52 週週線,是 walk-forward 完全合法的資訊集——真實交易者當下看盤本來就看得到。過去走勢是「輸入」,未來走勢才是「洩漏」。

唯一星號:在模型記憶區(2026-01 以前)內,豐富的歷史 K 線會幫模型「認出這是哪段時期」進而喚起對後續走勢的權重記憶——但該區間已永久放棄(見雙層記憶報告 §4),不構成反對理由。半乾淨區(2026-02~07-14)模型無該時期記憶可喚起,主線(前瞻)更無問題。**結論:此升級對主線與回測支線皆安全。**

## 3. v2 規格(定案)

```
【摘要 v2 結構】(對 as-of 時點生成,只含已收盤 K 線)
├─ 週線:最近 52 根 OHLCV(約 1 年結構)
├─ 日線:最近 90 根 OHLCV(約 3 個月)
├─ 4h:最近 42 根(約 7 天,盤中結構)
├─ 資金費率(永續):當期值 + 近 7 日均值
└─ 當下快照:現價、24h 變化、今日盤中高低(標明未收盤)
```

K 線以 CSV 文字表格呈現(`date,open,high,low,close,volume`),摘要總量約 5-8KB(≈ 每次判斷多 4-6k input tokens)。

### 三個設計原則

1. **中性資訊集**:給原始 K 線,不給預算好的結構(FVG/壓力位/均線值一律不算)。預算結構=把我們的解讀注入輸入;原始數據讓 ICT 自己看出 liquidity pool、讓均線派自己算均線——同一份輸入,各框架各自解讀,才測得出框架差異。這也是未來「資訊不對稱」設計的前置:先有共同基準,再談差異化切面。
2. **文字而非圖表**:文字 OHLCV 確定性、可重現,實盤與回測可產生位元級相同的輸入格式(可比性);圖表渲染多一層不確定性,不採用。
3. **生成器吃 as-of 參數**:同一支 generator,實盤傳今天、回測傳歷史日期。順帶把回測支線需要的「歷史摘要生成器」一次做掉。

### 納入與排除的決定

| 項目 | 決定 | 理由 |
|---|---|---|
| 資金費率 | **納入** | 客觀數據、ccxt 有歷史 API(`fetch_funding_rate_history`),回測可重現;對籌碼型人格是核心輸入 |
| 未平倉量(OI) | **v2 排除** | Binance API 的 OI 歷史僅回溯 30 天,回測窗口(2026-02 起)無法重現──違反「實盤與回測輸入一致」原則。未來資訊不對稱階段改用 Binance Vision 落地檔(quant 專案 `data_metrics/` 已有管線)再納入 |
| 今日未收盤 K 線 | 只放進「當下快照」段並標明未收盤 | 已收/未收嚴格分離,防止回測時混入當日資訊 |

### 回測模式的快照語義(開放問題,留給回測支線討論)

實盤模式快照=執行當下的即時價;歷史 as-of 模式無法重現「當天某時刻」的盤中狀態,v2 先以**該日開盤價**作為快照價(近似),並在摘要中標明。此近似的影響(實盤判斷含盤中資訊、回測判斷只到開盤)是實盤/回測可比性的已知落差,列入回測支線討論議程。

## 4. 治理:版本升級處理

- 輸入格式是「受測系統」的一部分,改輸入=改系統 → `PROTOCOL_VERSION` 升為 `v2-2026-07-19`
- 2026-07-19 的 2 筆(BTC/ETH)為 v1 樣本,DB 中 `protocol_version` 欄位可區分,主分析依版本過濾
- 升級時機:樣本 n=1 天,是最便宜的時點;預登記命中定義/聚合規則/標的/人格皆不動,以 §8 增補條目記錄(只增不改)
