# Phase 4 回測系統 — 規劃文件

> 依使用者要求，比對並改造 Obsidian `skills/quant skill/quant-strategy-dev` skill（v7，49 pytest 全過）的方法論，
> 產出交易員辯論系統 Phase 4 的回測設計。本文件只做規劃，尚未動工實作。
> **2026-07-19 更新**：skill 已迭代至 v13（118 pytest），v8→v13 新增機制的對齊結論見第 7 節。

---

## 0. 核心差異：這不是「策略回測」，是「預測準確率評估」

`quant-strategy-dev` 的評估對象是一個**有進出場規則、可調參數、會產生連續權益曲線的機械化策略**——所以它的核心武器是 Calmar/Sharpe 排序、DSR/PBO 抓參數過擬合、Kelly 部位管理、市場衝擊成本。

交易員辯論系統的 Phase 4 評估對象是**離散的方向預測（多/空/中性 + confidence 0-100）**，沒有進出場、沒有部位、沒有連續權益曲線，也（目前）沒有跨參數網格優化。這代表 quant skill 裡有一半左右的機制不能直接套用，需要換成分類/機率預測評估的對應工具。下面逐項比對。

---

## 1. 直接可複用（方法論相通，照搬邏輯）

| quant-strategy-dev 機制 | 對應到 Phase 4 的用法 |
|---|---|
| **Walk-Forward + Embargo**（Step 7, `oos-methods.md`） | 完全對應 WBS Phase 4.1 的「Walk-forward驗證」。Embargo 緩衝區的概念要保留：任何用來生成 bias 的資料（市場數據視窗）與被拿來驗證的實際結果之間，要留緩衝避免特徵計算跨界洩漏。 |
| **MCPT（Monte Carlo Permutation Test）**（Step 8） | 直接對應 WBS 4.4「對照隨機/簡單基準判斷是否有真實alpha」。做法：把「bias 判斷」與「實際結果」的配對順序隨機打散≥1000次，看真實命中率排在隨機分布的第幾百分位，取代單純算一個命中率就下結論。 |
| **Bootstrap 信賴區間** | 命中率、Brier score 等所有指標都應該給區間、不給單點數字——比照 quant skill「Sharpe 1.2，95% CI 0.3-2.1」的呈現方式，不要只寫「準確率62%」。 |
| **最低樣本數門檻＋明確排除**（Step 5 原則） | 任一人格、任一子分類（例如「高分歧日子」）樣本數不足時，直接標記「樣本不足，不列入評比」，不能為了湊數硬解讀。 |
| **敘事一致性審查**（Step 13, `consistency-review.md`） | 每次產出回測報告前，跑一次數字 vs 摘要文字的規則檢查（可仿照 `consistency_flags.py` 的模式），避免「準確率48%但摘要寫得很樂觀」這種落差。 |
| **部署後監控（CUSUM + tail_risk_drift_flag）**（Step 14） | 直接對應 WBS Phase 5.1「人格表現複檢，每月」——目前只是「人工複檢」，可以用 CUSUM 邏輯做成真正的偏移偵測：某人格的月度命中率相對於它自己的 OOS 基準期是否顯著往下漂移，而非靠人工肉眼比較。 |
| **模擬績效揭露聲明**（Step 12 開頭） | 每份 bias 準確率報告開頭都要註明「這是歷史回測的統計結果，不是實盤交易紀錄」，呼應 WBS 風險條款#5（研究工具，非自動交易訊號）。 |
| **業界函式庫交叉驗證**（Step 11） | 手算的 Brier score/校準曲線要跟 `sklearn.calibration`（`calibration_curve`、`brier_score_loss`）對一次，避免手刻公式出錯卻沒人發現——這正是 quant skill v5/v6 踩過的「死碼/公式bug」教訓，此處直接借用這個習慣。 |
| **邊界值測試 + 正式 pytest（而非只寫 smoke test）**（v6/v7 教訓） | Phase 4 任何新寫的統計 script，都要設計邊界案例（全部人格一致同意、confidence全部相同、樣本數=1、全部猜中/全部猜錯）並寫成 pytest，不要只靠 `if __name__` 手動跑一次。 |

---

## 2. 需要新增的方法論（此案例特有，quant skill 沒有對應工具）

這些是因為評估對象從「連續報酬策略」換成「機率化分類預測」而必須新增的：

### 2.1 信心校準（Calibration）
WBS 自己點名的核心指標「信心校準度」在 quant skill 裡沒有對應物（它評估的是報酬分布，不是機率預測的校準）。需要新增：
- **Brier Score**：`(confidence/100 - 實際是否命中)^2` 取平均，越低越好。
- **校準曲線（Reliability Diagram）**：把 confidence 分成幾個桶（如 0-20/20-40/.../80-100），比較每桶「平均宣稱信心」vs「該桶實際命中率」，理想狀況兩者應該接近對角線。
- 工具：直接用 `sklearn.calibration.calibration_curve` + `brier_score_loss`，不要手刻。

### 2.2 個別人格 vs 綜合辯論的提升度（Ensemble Lift）
quant skill 是單一策略評估，沒有「多個候選同時存在、要比較集成後是否更好」的機制。這裡需要新增：
- 逐日比對「辯論後綜合bias」的命中率 vs 每個人格「各自獨立判斷」的命中率。
- 用配對比較（例如 McNemar's test 或 paired bootstrap difference）而非單純比大小，因為樣本是同一批交易日、非獨立。
- **借用 quant skill 的一個關鍵洞察**：如果專案裡有5-6個人格，事後挑「這季表現最好的那個人格」來宣傳，本質上跟「測了50組參數挑最好的那組」是同一種多重比較問題——這裡應該借用 `effective_trials.py`／DSR 的精神，對「回測期內最佳單一人格」的顯著性做保守修正，不要不做校正就直接說「XX人格比其他都準」。

### 2.3 分歧情境表現（Disagreement-Conditional Accuracy）
WBS 4.4「分歧情境表現」目前也無對應工具。需要新增：
- 定義「分歧度」指標（例如各人格 direction 不一致的比例、或 confidence 標準差）。
- 依分歧度分桶（高/中/低），比較各桶的綜合bias命中率，驗證「分歧大的日子準確率是否真的較低」。
- 概念上可以借用 `oos-methods.md` 裡 regime-aware 切分的精神（依市場狀態分段，而非純時間切分）——這裡是「依分歧狀態分段」。

### 2.4 語料蒐集截止日的洩漏風險（本專案特有，quant skill 完全沒有這個問題）
這是**最重要的新增風險**，因為機械化策略沒有「訓練語料」這種東西，但人格蒸餾有：
- ICT 的 537 篇逐字稿涵蓋到 2026 年中（截至系統當前日期附近），且逐字稿內容本身就包含他對過去實際行情的評論。
- 若回測區間與語料涵蓋期重疊，人格有可能「記得」逐字稿裡提過的具體行情走勢，而非真的用框架推理——這是語言模型版本的look-ahead bias，比純數字策略的資料洩漏更隱蔽，很難用單純的 embargo 天數解決。
- **建議處理方式**：
  1. 回測窗口優先選在語料涵蓋範圍**之後**的日期（最乾淨，但等於要等新資料累積）。
  2. 若要往回測試涵蓋範圍內的日期，至少要做「洩漏稽核」：抽查該日期是否曾在537篇逐字稿中被具體提及（資產+方向+日期），若有明確提及該筆排除或另外標註，不能假裝沒這回事。
  3. 在報告的「模擬績效揭露聲明」中明確加一條這個專案特有的局限：語料截止日之前的回測結果，準確率可能因為語料記憶而虛高。

---

## 3. 不適用 / 明確延後（比照 quant skill「殘留風險要具體，不用空話」的原則）

| quant skill 機制            | 為何現在不適用                                     | 未來重新評估的觸發條件                                                                              |
| ------------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------- |
| DSR / PBO（多重比較過擬合校正，原始用途） | 目前 bias 產出沒有數值參數網格優化，沒有「測了50組挑最好」的動作        | 若未來裁判/加權層開始用歷史準確率**數值優化**人格權重（等於在對歷史資料做參數搜尋），必須套用 DSR/PBO 邏輯避免權重過擬合                      |
| Kelly 公式／部位管理             | Phase 4 評估的是預測品質，不是執行後的報酬，目前沒有部位/下單層        | 若之後把 confidence 轉換成實際部位大小建議（等於做出「自動化交易訊號」），這違反 WBS 自訂的專案定位（研究工具，非自動交易），需要使用者明確決定要不要跨過這條線 |
| 非線性市場衝擊成本／滑價              | 沒有模擬下單和成交，無成本可估                             | 同上，只有進到實際部位模擬時才需要                                                                        |
| 跨資產穩健性檢查（Step 4原意）        | 目前只有 ICT 一個人格、BTC一個資產，沒有「同邏輯套到另一個相關商品」的比較對象 | 未來多資產（如同時測NQ/ES）或多人格上線後，可比照精神做「同一人格換資產是否還準」的穩健性檢查                                        |
| 投組層級相關性檢查（Step 10）        | 目前只有一套辯論輸出，沒有「使用者已有其他策略在跑」的情境               | 若之後這個 bias 訊號要跟使用者 `quant/` 資料夾裡其他既有策略（如 AMD策略、MTF-FVG-MSS-SMT）組成投組，才需要算相關係數與邊際風險貢獻      |

---

## 4. Schema／資料落地缺口（規劃層面必須先解決，才有東西可以回測）

檢查現有 `database/schema.py`，發現關鍵缺口：**三張表（`market_data`/`persona_debates`/`daily_bias_results`）目前都沒有「事後結果」欄位**——沒有 `price_after_1d/5d/20d`，也沒有「這次判斷是否命中」的欄位。這跟 WBS 文件 Phase 1.2 原始設計的 `posts` schema 精神一致但從未真正落地。

需要新增（規劃層級，實作時再細談）：
- `daily_bias_results` 需要補上：`price_at_bias`（判斷當下價格）、`price_after_1d`/`price_after_5d`/`price_after_20d`（事後價格，用於判定命中與否）、`realized_direction`（依價格變動反推的實際方向）、`is_correct`（命中與否，可由前述欄位算出，不必是使用者手填）。
- `persona_debates` 同樣需要能回推「這個人格這次判斷對不對」的欄位，才能做 2.2 的逐人格命中率比較。
- 新增一張 `backtest_runs` 或 `calibration_reports` 表，記錄每次跑 Phase 4 分析的區間、樣本數、MCPT p-value、Brier score 等，避免每次都要重新從頭算。

---

## 5. 建議實作順序

1. **先補 schema 缺口**（第4節）——沒有事後結果欄位，後面全部都是空談。
2. **把市場數據抓取與 bias 產出串起來，開始每日/每週累積真實紀錄**（目前 `main.py` 連 DB 都沒寫入，這個要先修，見前面 Phase 3 的討論）。
3. **等累積到足夠樣本後，先做最基礎的方向命中率 + Brier Score/校準曲線**（2.1），這是最簡單也最快能看到訊號有沒有意義的指標。
4. **确认語料洩漏風險的處理方式**（2.4）——這一步要跟使用者一起決定用哪種策略（等新資料 vs 洩漏稽核），因為會直接決定「現在能不能回測」還是「要等幾個月」。
5. **有多人格之後再做 2.2（ensemble lift）跟 2.3（分歧情境表現）**——目前只有 ICT 一個真正蒸餾完成的人格，這兩項暫時做不了，屬於「多人格上線後」的後續項目。
6. **借用 quant skill 的工程紀律**：新寫的每支評估 script 都要有對應 pytest、都要設計邊界案例，比照 `quant-strategy-dev` v6/v7 的教訓，不要重蹈「死碼三個版本沒被發現」的覆轍。

---

## 6. 已知限制（誠實列出，比照 quant skill 慣例）

- 目前只有 ICT 一個人格完成真正蒸餾，2.2/2.3 兩項多人格比較指標實質上還無法執行。
- 語料洩漏風險（2.4）目前沒有自動化稽核工具，只能人工抽查，且「抽查沒發現明確提及」不等於「確定沒有洩漏」，只是降低風險。
- 本文件的 MCPT/Brier/校準曲線設計都還停留在規劃層級，尚未寫成程式碼或跑過任何 smoke test，不能視為已驗證可用。
- 是否要之後把 DSR/PBO 邏輯搬進來校正「多人格選最佳」的比較，取決於使用者未來會不會做人格權重的數值優化——如果加權層設計最終只是簡單規則（如「歷史準確率前2名各占50%」），不涉及數值搜尋，則不需要引入 DSR/PBO。

---

## 7. v8→v13 對齊增補（2026-07-19）

本文件原以 v7 為基準；quant skill 現已迭代至 v13（118 pytest 全過）。逐版檢視 v8-v13 的新增機制，對 Phase 4 有實質影響的五項如下——無影響者不硬湊關聯。

### 7.1 假設預登記（v11）——影響最大，改變實作順序
v11 起 quant skill 規定「無假設登記拒跑網格」。對應到 Phase 4：**在第一筆每日 bias 落地之前**，必須先預登記「命中定義」，包含：
- 主評估地平線（建議 1d，理由見 7.2）
- Neutral 的判定規則（例：|1d 漲跌幅| < X% 時 Neutral 算命中，門檻 X 預先寫死）
- 首次評估的最低樣本數門檻
- 通過/失敗的預宣告標準

落地方式：寫成一份帶日期戳記的 `preregistration.md`（或存入 §4 規劃的 `backtest_runs` 表），之後不可改；要改就重新起算樣本。防的是「等資料累積完再挑對自己有利的命中定義」——與 quant skill 防「事後改標準」完全同源。**這一項應插入第 5 節實作順序的步驟 1 與 2 之間。**

### 7.2 預測地平線匹配（v12 Phase 1B）
v12 的地平線錯配教訓（秒-分鐘級證據拿到小時級用是錯配，必須明講）直接適用：ICT 的 Daily Bias 語意上是「今日」的方向判斷，主評估地平線應為 **1d**；5d/20d 只能作為次要/探索性指標，且要在預登記中明確標註身分，不可事後挑表現最好的地平線來宣傳。

### 7.3 重疊視窗的序列相關（v9 IID 檢查的對應問題）
若每日出 bias 且同時評估 5d/20d 地平線，相鄰交易日的結果視窗大量重疊，命中序列**非獨立**——naive binomial CI 與普通 bootstrap 都會低估不確定性（對應 quant skill v9 引入 Ljung-Box/Lo 2002 的同一教訓：序列相關讓顯著性虛高）。對策：
- 20d/5d 地平線改用非重疊抽樣或 block bootstrap 計算 CI
- 1d 地平線受影響最小——這是 7.2 選 1d 作主地平線的另一個理由

### 7.4 標準報告強制化（v10 + v13 兩次驗證的「模板≠執行」病灶）
v10（MCPT 圖）與 v13（指標表）兩次驗證同型病灶：**指標列在模板不會被執行，強制字句＋現成 script 才會**。Phase 4 直接繼承此教訓：
- 寫一支 `bias_report_metrics.py`：單一呼叫產出強制指標表（樣本數、各地平線命中率＋bootstrap CI、Brier score、校準分桶表、MCPT p-value、分歧度統計）＋標準圖（校準曲線、滾動命中率、MCPT 排列分布圖）
- 規則比照 v13 Step 12：**缺表缺圖＝報告未完成；指標逐項照列，不可自選子集**
- §1 原寫「比照 consistency_flags 的模式」屬建議性質，現升級為與 v13 同等的強制規格

### 7.5 多人格比較改用現成 BH-FDR（v11 script）
§2.2 原寫「借用 DSR 的精神做保守修正」；v11 之後有現成工具：`multiple_testing.py` 的 `benjamini_hochberg()` 可直接搬來，對「N 個人格中挑最佳」的顯著性做 FDR 控制（HLZ t≥3 假設 IID，受 7.3 同樣限制，只能當粗篩）。多人格上線後適用，目前只有 ICT 一個人格，暫不需要。

### 檢視後判定無需變動的項目
- §3 不適用清單（DSR/PBO 原始用途、Kelly、衝擊成本、跨資產、投組相關性）在 v13 下判斷不變。
- v9 凍結保留區：每日前瞻累積的資料天然是「新鮮資料」，無歷史段可封存；若未來決定回測語料涵蓋期（§2.4 選項 2），再引入保留區設計。
- v12「需求不可靜默漂移」：對應本專案＝「研究工具、非自動交易訊號」的定位每份報告重述，§1 的揭露聲明已涵蓋。
