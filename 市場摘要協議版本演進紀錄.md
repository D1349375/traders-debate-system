# 市場摘要協議版本演進紀錄(v1 → 現行)

> **這份文件的用途**:單一入口回答「`protocol_version` 每一版改了什麼、為什麼改」,不用在 `TODO.md`(散在各處的完成項)、`preregistration.md` §8(逐條增補、按時間序不按主題)、程式碼註解(只有一行版本摘要)之間東拼西湊。
> **維護規則(重要,任何後續版本都要遵守)**:每次 `main.py` 的 `PROTOCOL_VERSION` 升版,**同一次改動內**必須在本檔案補一節,格式比照下方既有版本。這是「思路」的權威來源;完整技術規格與測試覆蓋的權威來源仍是程式碼本身與 `preregistration.md` §8(那裡的用詞更正式、逐字記錄變更當下的決策脈絡,本檔案是給後續讀者/session 用的濃縮導覽)。
> 相關文件:[市場摘要v2_資訊集設計.md](市場摘要v2_資訊集設計.md)(v2 的完整設計報告,本檔案 v2 一節只摘要,細節見該檔)、[preregistration.md](preregistration.md) §8(逐條正式記錄)、[回測污染分析_雙層記憶.md](回測污染分析_雙層記憶.md)。

---

## v1 — 2026-07-19(首版,已淘汰)

**內容**:只給 24 小時切片(現價/今日高低/昨收/成交量)。

**問題(當天就發現)**:ICT 這類人格的框架本質是 HTF 敘事驅動——日線/週線 PD array 決定 bias,LTF 只是進場擇時。只給 24h 切片等於要求人格在被剝奪核心工具的狀態下判斷。首筆紀錄裡 ICT 自己抱怨「四個維度我只摸到半個」,給出 Neutral/25 的低信心。**這個版本測的是「框架被砍掉 HTF 後還剩多少」,沒有價值,當天就決定升版。**

樣本:2026-07-19 的 2 筆(BTC/ETH)為 v1 樣本,`protocol_version` 過濾,不與後續版本混算。

---

## v2 — 2026-07-19 — 中性資訊集升級

**動機**:直接修正 v1 的 HTF 缺失問題。

**改了什麼**:摘要從單一切片改成多時間框架已收盤 K 線(週 52 根、日 90 根、4H 42 根)+ 資金費率(當期+近7日均值)+ 當下快照(未收盤,獨立標示)。

**三個關鍵設計原則**(完整推導見 [市場摘要v2_資訊集設計.md](市場摘要v2_資訊集設計.md)):
1. **中性資訊集**:給原始 K 線,不給預算好的結構(FVG/壓力位/均線值一律不算)——讓 ICT 自己看出 liquidity pool、讓均線派自己算均線,同一份輸入各框架各自解讀,才測得出框架差異。**這是後續 v4/v5 資訊分流的前置原則,從 v2 就定調了。**
2. **文字而非圖表**:確定性、可重現,實盤與回測產生位元級相同格式。
3. **生成器吃 as-of 參數**:同一支 generator,實盤傳 None(=now)、回測傳歷史日期,順便把回測支線需要的歷史摘要生成器一次做掉。

**為什麼補歷史 K 線不算記憶污染**:look-ahead bias 的定義是「判斷時用到判斷日**之後**的資訊」。給判斷日**之前**的走勢是 walk-forward 合法輸入,真實交易者當下看盤本來就看得到——過去走勢是輸入,未來走勢才是洩漏。

**排除項**:未平倉量(OI)——Binance API 歷史僅回溯 30 天,回測窗口無法重現,違反實盤/回測輸入一致原則,留給未來資訊不對稱階段接 Binance Vision 落地檔再議。

---

## v3 — 2026-07-20 — 日內時間框架 + `intraday_scenario` 必填

**動機**:dry-run 觀察到地平線錯配——只有週/日/4H 時,人格傾向給出中週期波段判斷,跟主評估地平線 1d 脫鉤。

**改了什麼**:
1. 新增 1H(48根≈2天)/15M(96根≈1天)/5M(48根≈4小時)三個日內時間框架。K 線總數 184→370 根,摘要 12.2KB→24.2KB。5M 刻意限縮在 4 小時而非整天,避免與 15M 大量重疊、徒增成本卻無新資訊。
2. `persona_debates` 新增 `intraday_scenario` 必填欄位(R1/R2 皆強制):判斷日當天收盤前的雙劇本 if-then,範圍嚴格限定當天,不得是多日/週目標。`record_opinion()` 留空直接拒絕落地。

**同批附帶決策**:
- 三人格正式生效(ICT+TJR+EmperorBTC),R2 結構化反駁自動啟用。
- 執行時間紀律定案:UTC 00:00-01:00,窗口錯過當日盡快補跑不跳過,真死線是隔日 UTC 00:00。
- ICT/TJR SKILL.md 新增「資料使用邊界(硬性)」,明文禁止引用成交量——這是 v4 真正資訊分流的伏筆,此時還只是「看得到但不准用」。
- TradingView 等外部資料源評估後否決(破壞回測/實盤一致性、疊加視覺判讀雜訊),維持 Binance/ccxt 架構。

---

## v4 — 2026-07-21 — 真正的資訊分流(core/emperorbtc)

**動機**:與外部協作 agent 討論產出的兩份報告點出「五人格同一顆 LLM 讀同一份摘要」的統計層獨立性問題(流程層獨立已做到,統計層獨立沒有)。經診斷,v3 的「看得到成交量但規定不用」是半吊子解法——真正該做的是資料層面就不存在。

**改了什麼**:摘要拆成兩個變體,不再是單一共用文字:
- `core`(原 ICT/TJR 共用,v5 起改 ICT 專屬):K 線 CSV **不含成交量欄位**,連 header 都不出現。
- `emperorbtc`(專屬):含成交量 + 新增「量能與動能指標」區塊——RSI(14,Wilder 標準公式)+ 近 7 日均量 + 當前量/均量比值,皆程式碼算出的客觀公式值。

**兩個明確拒絕的候選項(定調了本專案資訊分流的判斷準則)**:
- **POC/value area**:需要決定 lookback window、成交量分箱粒度等方法論參數,不同選法會算出不同位置——**這已經是一次方法論詮釋,不是客觀事實抽取**,由主 agent 先選定參數算好等於替人格做了一次框架選擇,牴觸「詮釋權完整下放」原則。
- **Swing high/low 預先計算**:抽查 2026-07-20 凍結行情原始 K 線與當日報告引用的三個具體價位,精確吻合、無任何計算誤差——沒有實證問題,不做無謂的過度優化。

**判準**(後續版本沿用):只計算「無方法論爭議的單一公式」(RSI、均量比值屬此類),不計算「涉及參數選擇的方法論建構」(POC 屬此類)。

**同批**:`database/db.py` 的 `get_session()` 新增自動 schema 遷移(`_sync_schema()`)——此欄位手動遷移已做過三次,自動化後協作者 pull 新 schema 不再需要手動 `ALTER TABLE`。

---

## v5 — 2026-07-22 — 總經行事曆旗標 + TJR 專屬 SMT 參考行情 + ICT 信心天花板修正

**動機**:使用者指出 ICT 連續多日 R1 判斷都因「缺 COT/SMT」自砍信心,懷疑信心分佈失真、影響回測有效性;同時討論 TJR 是否該拿到跨資產 SMT 所需的資料。

**三項變更**:

1. **總經行事曆旗標**(三變體皆含,共用中性事實層):NFP 週(規則計算,任何年份皆準)、FOMC 決策週(僅涵蓋 2026 年,來源 federalreserve.gov,**需逐年手動更新**)、8月旗標。純日期規則,不需外部即時查詢。誠實揭露涵蓋範圍不含 CPI/假期行事曆。

2. **新增 `tjr` 變體**(取代原 ICT/TJR 共用的 `core`):core 內容 + 相關資產(BTC↔ETH)日/4H/1H/15M 參考行情,不含成交量,不精算 divergence 結論。**ICT 刻意不給同等資料**——判準:TJR SKILL.md 明文要求查「BTC vs ETH SMT 背離」(有語料依據),ICT 語料裡 SMT 只舉 ES/NASDAQ、從未提過 BTC/ETH(給了也未必會用,主動提示等於替他發明語料沒有的框架連結)。這是隔離鐵律的明確窄範圍例外,已知後果:BTC/ETH 的 TJR 判斷此後不再是完全獨立樣本,Phase4 分析需考慮這個跨資產相關性。

3. **ICT 信心分級表修正**:COT 與跨資產 SMT 在本系統結構性永久缺席(不是「今天恰好缺」),但原始信心表把「COT/季節性/SMT」三個子項綁在同一個「維度4」,導致維度4永遠不可能滿足,信心永久鎖死在 45-70 區間,75-95 頂格區間實質上永遠打不開——這是計分規則的結構性天花板,不是判斷力保守,每天都會發生、不會隨樣本數增加自然抵銷。修正:拆分「COT/SMT 永久缺席不計入維度4達成」與「季節性判斷不受此限制,可正常依總經行事曆旗標評估」。

**同批**:`trader-debate/SKILL.md` 前置檢查新增 FOMC 年份覆蓋範圍檢查——判斷日進入 2027 年後,Step 5 回報使用者時主動提醒手動更新 `_FOMC_MEETINGS_2026` 常數,不靠人記得。

**討論但明確不做(已列入 TODO.md「未來方向」,標記非必須)**:消息面 compiler agent——真正不可預期的即時消息(突發新聞、非排定 Fed 談話)目前完全沒有涵蓋,但隱藏成本高(WebSearch 延遲/token成本/來源可靠性)、已知行事曆已解掉多數已被 SKILL.md 提及的用例、沒有證據顯示現有樣本因此判斷失準,優先要務仍是 `bias_report_metrics.py` 與樣本累積。

**首次實測驗證**(2026-07-22 正式樣本):ICT R1 明確引用「不會因為COT/SMT結構性拿不到就自砍信心」;TJR R1 首次實際引用 ETH 對照(「拿ETH对照一下,它今天也是同一时间见顶同一时间往下掉,两边同步、没有SMT背离」)。

---

## 貫穿全版本的固定判準(給未來版本參考)

- **中性資訊集原則**(v2 定調):給客觀事實,不給預算好的詮釋,詮釋權留給人格框架。
- **只算無方法論爭議的公式,不算涉及參數選擇的方法論建構**(v4 定調,POC vs RSI/swing-high-low 的分野)。
- **資訊分流要有語料依據,不能主動替人格發明框架延伸**(v5 定調,TJR vs ICT 的 SMT 資料落差)。
- **沒有實證問題不預先修**(v4 定調,swing high/low 抽查驗證後才決定不做)。
- **任何改變輸入/協議的變更 = 改變受測系統**:`PROTOCOL_VERSION` 必升版,`preregistration.md` §8 必記一筆,樣本依版本過濾分析,不可跨版本混算。
