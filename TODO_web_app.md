# 網站／App 介面開發 — 獨立 Todolist

> 與核心研究/驗證系統的 `TODO.md` 分開追蹤。此檔案只管「使用者層介面怎麼做」，不影響 `bias_report_metrics.py`、預登記、辯論協議等核心驗證邏輯的優先順序。
> 討論脈絡見 2026-07-23 `debate_sys-` session 與本 session。

## 現況結論（2026-07-23 討論定案）

- **核心判斷仍是「先驗證再包裝」，未變**：目前樣本 n=10（需 n≥30 才有首份命中率報告、n≥60 才能做統計檢定），`bias_report_metrics.py` 尚未實作，還不知道三人格聚合有沒有真實 edge。
- **但價值判斷已釐清**：這個系統現階段的價值是「逼交易員用結構化方式思考」（明確的進場/停損/目標/失效條件），這個價值不依賴回測準確度，回測結果不佳也不會讓它消失。
- **因此結論是**：網站可以現在就先做「私人用」版本（風險低、練手用途），但「對外公開」要等 `bias_report_metrics.py` 產出、有真實準確度數字後再決定——屆時才知道免責聲明該怎麼寫才誠實。
- **App 現階段不需要**：技術上網站已足夠涵蓋現有需求；App 的主要誘因（推播提醒）其實 Web Push/PWA 已可做到，不必為此換路線。

---

## 網站（近期可做，風險低）

- [ ] 把現有 `data/reports/*.md`（或 `.html` 版型）發布成網頁——**先做私人用版本，不對外開放**
- [ ] 免責聲明必須跟信心分數（如「Bullish 68/100」）同樣顯眼，避免使用者誤讀成校準過的勝率
- [ ] 對外公開的時間點：等 `bias_report_metrics.py` 完成 + n≥30 有真實準確度數字後再評估，屆時同步決定免責聲明的最終措辭
- [ ] 技術選型待評估（純靜態發布 md/html vs. 簡單後端讀 SQLite 現算），優先選「迭代快、不鎖死呈現方式」的做法，因為免責聲明/UI 措辭現階段還會反覆調整

## App（未來方向，非現階段）

- [ ] **推播通知功能的觸發點**：例如「ICT 關注的進場價位到了」提醒
  - **技術筆記**：不必為了推播單獨做原生 App——Web Push API（iOS 16.4+ Safari、Android Chrome 皆支援）在 PWA 上已可做到同等推播體驗；要不要做原生 App 應由其他考量決定（上架、更完整原生體驗等），推播本身不是門檻
  - **需要新增的基礎設施**：現有系統是每日批次一次（UTC 00:00-01:00 抓一次行情），「價位到了即時通知」需要另外做一個持續輪詢即時價格、比對 `trade_plan` 欄位（進場/停損/目標）的常駐監控服務——這是新的基礎設施量級，不是現有批次架構的小擴充
  - **⚠️ 定位紅線警示**：目前 `trade_plan` 是敘述性資訊，使用者自己看報告判斷要不要行動；「價位到了主動推播提醒」性質上更接近「主動觸發交易動作的訊號」，而不是單純的輔助思考工具。呼應 `README.md` 已記錄的定位紅線（confidence 轉部位建議 = 跨入自動交易訊號，需使用者明確決定）——這個推播功能要做之前，必須先明確想清楚是否要跨這條線，不能當成單純的 UI/推播技術功能就直接上線
- [ ] 其他可能結合的功能（待補充，目前僅在概念階段，未展開設計）

---

## 開放問題（尚未決定，留待之後討論）

- [ ] 網站對外公開後的呈現規格：信心分數旁邊要放多明顯的免責聲明？要不要強制附上「這不是校準過的機率」之類的字樣？
- [ ] App 若真要做，是原生 App 還是維持 PWA 形式（PWA 已可涵蓋推播需求，原生的追加價值是什麼）？
- [ ] 推播的「定位紅線」問題最終怎麼決定：要不要做、做的話怎麼避免變成事實上的交易訊號產品？
